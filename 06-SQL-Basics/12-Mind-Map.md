# 🧠 Mind Map

SQL Basics
│
├── SELECT
│
├── WHERE
│
├── ORDER BY
│
├── DISTINCT
│
├── LIMIT
│
├── GROUP BY
│
├── HAVING
│
└── SQL Execution Flow
       │
       ├── FROM
       ├── WHERE
       ├── GROUP BY
       ├── HAVING
       ├── SELECT
       ├── ORDER BY
       └── LIMIT